import React from 'react';
import PropTypes from 'prop-types';

const ToolControllerText = ({ }) => {
    return (
        <div className="editorMain">
          text 
        </div>
    );
};

ToolControllerText.propTypes = {
   
};
export default ToolControllerText;
